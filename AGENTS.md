# AGENTS.md

All style and workflow law for this repo lives in the Claude Code rule files at `.claude/rules/` (code-patterns, conventions, react-patterns, worker-d1) and in `CLAUDE.md`. Non-Claude tools: read those files before writing or reviewing code here.

Commands: `pnpm verify` before every commit; `pnpm dev` (local D1); `pnpm db:migrate:local` once per clone; `pnpm seed:qa` for test data.
